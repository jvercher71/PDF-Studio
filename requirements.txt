# Zeus PDF — runtime dependencies
# For development, install with: pip install -e ".[full,dev]"
#
# This file exists for quick pip installs and CI caching. It mirrors the
# runtime dependencies in pyproject.toml. Optional extras (signing, docx,
# xlsx, dev, build) are declared there.
PySide6>=6.5.0
PyMuPDF>=1.23.0
pypdf>=3.0.0
Pillow>=10.0.0

# Optional — cryptographic signing
pyHanko>=0.21.0
pyhanko-certvalidator>=0.26.0
cryptography>=41.0.0

# Optional — format conversion
pdf2docx>=0.5.7
openpyxl>=3.1.0
